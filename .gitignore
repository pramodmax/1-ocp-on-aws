# Terraform state — use remote backend for teams
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl
.history
# Sensitive variable files — never commit
terraform.tfvars
*.auto.tfvars

# OpenShift install directory — contains kubeconfig, pull-secret, private keys
clusters/

# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp
